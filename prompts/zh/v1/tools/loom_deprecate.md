# 工具说明

将条目标记为 deprecated：保留历史但标明过时，可选指向替代条目。

## 参数：category

`concepts` | `decisions` | `threads`

## 参数：slug

要废弃条目的 slug（无 `.md` 后缀）。

## 参数：reason

废弃原因（例如已被新方案替代）。

## 参数：superseded_by

可选：替代条目路径（如 `concepts/new-auth-flow`）。
