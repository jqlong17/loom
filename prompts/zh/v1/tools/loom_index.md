# 工具说明

读取 Loom 索引与「必读集合」。渐进披露顺序建议：index → trace → read。

正文末尾 **「### Full Index」** 为 `index.md` 重建结果；默认 **最多展示 `indexFullMaxChars` 字符**（默认 16000，可由 `mcpReadLimits.indexFullMaxChars` 或 `LOOM_MCP_INDEX_FULL_MAX_CHARS` 调整）。超长时会截断并提示用 `loom_read` / `loom_trace` 缩小范围。
