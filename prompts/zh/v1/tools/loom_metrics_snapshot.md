# 工具说明

生成指标快照 JSON（治理与辅助观测）。

## 参数：snapshot_date

快照日期 `YYYY-MM-DD`，默认当天。

## 参数：stale_days

doctor 陈旧天数（可选）。

## 参数：include_threads

doctor 是否包含 threads（可选）。

## 参数：max_findings

doctor 最多返回条数（可选）。

## 参数：fail_on

doctor 门禁：`none` | `error` | `warn`（可选）。
